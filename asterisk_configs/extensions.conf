[ai-routing]
exten => _X.,1,NoOp(Incoming call from NeoX PBX to Python AI)
same => n,Answer()
; Route the raw audio to our Python AudioSocket Server on port 9090
same => n,AudioSocket(11111111-2222-3333-4444-555555555555,127.0.0.1:9090)
same => n,Hangup()
